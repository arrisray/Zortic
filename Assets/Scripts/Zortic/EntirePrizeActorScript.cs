/*

private int animNum = 1;

public void Start()
{
	UpdateAnim();
}

public void UpdateAnim()
{
	if(animNum > 6)
		animNum = 1;

	if(animNum == 1)
	{
		animation.Play("EntirePrize@flyby1");
		yield WaitForSeconds (animation.clip.length + 2.0f);
		animNum++;
	}
	if(animNum == 2)
	{
		animation.Play("EntirePrize@flyby2");
		yield WaitForSeconds (animation.clip.length + 2.0f);
		animNum++;
	}
	if(animNum == 3)
	{
		animation.Play("EntirePrize@flyby3");
		yield WaitForSeconds (animation.clip.length + 2.0f);
		animNum++;
	}
	if(animNum == 4)
	{
		animation.Play("EntirePrize@flyby4");
		yield WaitForSeconds (animation.clip.length + 2.0f);
		animNum++;
	}
	if(animNum == 5)
	{
		animation.Play("EntirePrize@flyby5");
		yield WaitForSeconds (animation.clip.length + 2.0f);
		animNum++;
	}
	if(animNum == 6)
	{
		animation.Play("EntirePrize@flyby6");
		yield WaitForSeconds (animation.clip.length + 2.0f);
		animNum++;
		Start();
	}
}

*/
