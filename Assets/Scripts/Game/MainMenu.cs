
/*
private float screen;

String level01Name;
int level01LevelLoad;

GUIStyle levelStyle = new GUIStyle();

public void OnGUI()
{	
	//screen format
	if(Screen.width > Screen.height)
		screen = Screen.width;
	else
		screen = Screen.height;
		
	if(GUI.Button(Rect(0,0,Screen.width/2, Screen.height/20),level01Name, levelStyle))
		Application.LoadLevel(level01LevelLoad);
}
*/